export class Features {
  // tslint:disable: variable-name
  _id: string;
  descript: string;
  uses: number;
  uses_left: number;
  listof_atks: [string];
  listof_saves: [string];
  listof_featureprofs: [string];
}
