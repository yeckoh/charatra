export class SpellList {
  // tslint:disable: variable-name

  selected_color: string;

  _id: string;
  name: string;
  saveDC: string;
  atk_bonus: string;
  max_prepared: number;
  listof_spells: [string];
}
