export class Items {
  // tslint:disable: variable-name

  selected_color: string;

  _id: string;
  descript: string;
  weight: number;
  value: number;
  attunement: boolean;
  // equipped: boolean;
  listof_itemsfeatures: [string];
  listof_spells: [string];
}
