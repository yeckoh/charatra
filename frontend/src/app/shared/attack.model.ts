export class Attack {
    // tslint:disable: variable-name

  selected_color: string;
  is_active: boolean; // can we autohide if its from an item and unequipped

  _id: string;
  // ownerfeature_id: string;
  // ownerchara_id:, <-- for stats{} ?
  name: string;
  details: string;
  atkbonus: string;
  damage: string;
}
